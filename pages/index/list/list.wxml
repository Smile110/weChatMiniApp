<!--pages/index/list/list.wxml-->
<view>
  <view class='list-item' wx:for-items="{{list}}" wx:key="{{item.id}}">
    <image class="list-img" src="resources/kind/{{item.id}}.png"></image>
    <view class='list-title'>{{item.name}}</view>
  </view>
</view>
